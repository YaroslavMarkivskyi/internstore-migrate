import { styled } from '@mui/material/styles';

// We don't need specific styles for this component right now,
// but this file is created to maintain consistency with our folder structure
// and to allow for future styling enhancements

export const DragOverlayContainer = styled('div')({
  // Base styling for the drag overlay container if needed in the future
});
