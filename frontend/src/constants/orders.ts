import colors from './colors';

export const orderStatusColors = {
  new: colors.error400,
  pending: colors.warning100,
  paid: colors.success700,
  cancelled: colors.text700,
  rejected: colors.text200,
  done: colors.success100,
};
