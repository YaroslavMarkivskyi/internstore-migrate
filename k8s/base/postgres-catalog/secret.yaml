# DEV-ONLY: plaintext values committed for local kind/minikube use only.
# See k8s/README.md "Secrets" section.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-catalog-secret
  labels:
    app: postgres-catalog
type: Opaque
stringData:
  POSTGRES_DB: "catalog"
  POSTGRES_USER: "catalog"
  POSTGRES_PASSWORD: "catalog"
