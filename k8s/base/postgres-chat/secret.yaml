# DEV-ONLY: plaintext values committed for local kind/minikube use only.
# See k8s/README.md "Secrets" section.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-chat-secret
  labels:
    app: postgres-chat
type: Opaque
stringData:
  POSTGRES_DB: "chat"
  POSTGRES_USER: "chat"
  POSTGRES_PASSWORD: "chat"
