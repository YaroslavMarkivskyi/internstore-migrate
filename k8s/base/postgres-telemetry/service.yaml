apiVersion: v1
kind: Service
metadata:
  name: postgres-telemetry
  labels:
    app: postgres-telemetry
spec:
  selector:
    app: postgres-telemetry
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
