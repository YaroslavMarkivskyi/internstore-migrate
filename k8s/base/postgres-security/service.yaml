apiVersion: v1
kind: Service
metadata:
  name: postgres-security
  labels:
    app: postgres-security
spec:
  selector:
    app: postgres-security
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
