apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-security
  labels:
    app: postgres-security
spec:
  serviceName: postgres-security
  replicas: 1
  selector:
    matchLabels:
      app: postgres-security
  template:
    metadata:
      labels:
        app: postgres-security
    spec:
      enableServiceLinks: false
      containers:
      - name: postgres
        image: postgres:16-alpine
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5432
        envFrom:
        - secretRef:
            name: postgres-security-secret
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
          # Postgres refuses to initdb into a non-empty mount point that
          # also contains the volume's own lost+found dir — subPath sidesteps
          # that without needing an initContainer, same net effect as
          # compose's named volume (which mounts a dedicated dir, not the PVC
          # root) mapping straight to /var/lib/postgresql/data.
          subPath: postgres
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "security"]
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          exec:
            command: ["pg_isready", "-U", "security"]
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: postgres-security-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-security-data
  labels:
    app: postgres-security
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
