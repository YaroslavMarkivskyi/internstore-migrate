apiVersion: apps/v1
kind: Deployment
metadata:
  name: security
  labels:
    app: security
spec:
  replicas: 1
  selector:
    matchLabels:
      app: security
  template:
    metadata:
      labels:
        app: security
    spec:
      enableServiceLinks: false
      containers:
      - name: security
        image: internstore/security:local
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: security-config
        - secretRef:
            name: security-secret
            optional: true
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
      - name: security-opa
        image: openpolicyagent/opa:latest
        imagePullPolicy: IfNotPresent
        args:
        - run
        - --server
        - --addr=:8181
        - /policies/security.rego
        - /policies/common.rego
        env:
        - name: INTERNAL_TOKEN_SECRET
          valueFrom:
            secretKeyRef:
              name: security-secret
              key: INTERNAL_TOKEN_SECRET
              optional: true
        volumeMounts:
        - name: policies
          mountPath: /policies
          readOnly: true
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      - name: security-verify
        image: internstore/internal-gate:local
        imagePullPolicy: IfNotPresent
        env:
        - name: PORT
          value: '8090'
        - name: OPA_URL
          value: http://localhost:8181
        - name: OPA_PACKAGE
          value: security
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      - name: security-gate
        image: nginx:1.27-alpine
        imagePullPolicy: IfNotPresent
        command:
        - nginx
        - -c
        - /etc/nginx-src/security.conf
        - -g
        - daemon off;
        ports:
        - containerPort: 8000
        volumeMounts:
        - name: security-gate-config
          mountPath: /etc/nginx-src
          readOnly: true
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 10
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      volumes:
      - name: policies
        configMap:
          name: opa-policies
      - name: security-gate-config
        configMap:
          name: security-gate-config
