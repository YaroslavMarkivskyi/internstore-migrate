apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetry
  labels:
    app: telemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telemetry
  template:
    metadata:
      labels:
        app: telemetry
    spec:
      enableServiceLinks: false
      containers:
      - name: telemetry
        image: internstore/telemetry:local
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: telemetry-config
        - secretRef:
            name: telemetry-secret
            optional: true
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
      - name: telemetry-opa
        image: openpolicyagent/opa:latest
        imagePullPolicy: IfNotPresent
        args:
        - run
        - --server
        - --addr=:8181
        - /policies/telemetry.rego
        - /policies/common.rego
        env:
        - name: INTERNAL_TOKEN_SECRET
          valueFrom:
            secretKeyRef:
              name: telemetry-secret
              key: INTERNAL_TOKEN_SECRET
              optional: true
        volumeMounts:
        - name: policies
          mountPath: /policies
          readOnly: true
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      - name: telemetry-verify
        image: internstore/internal-gate:local
        imagePullPolicy: IfNotPresent
        env:
        - name: PORT
          value: '8090'
        - name: OPA_URL
          value: http://localhost:8181
        - name: OPA_PACKAGE
          value: telemetry
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      - name: telemetry-gate
        image: nginx:1.27-alpine
        imagePullPolicy: IfNotPresent
        command:
        - nginx
        - -c
        - /etc/nginx-src/telemetry.conf
        - -g
        - daemon off;
        ports:
        - containerPort: 8000
        volumeMounts:
        - name: telemetry-gate-config
          mountPath: /etc/nginx-src
          readOnly: true
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 10
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      volumes:
      - name: policies
        configMap:
          name: opa-policies
      - name: telemetry-gate-config
        configMap:
          name: telemetry-gate-config
